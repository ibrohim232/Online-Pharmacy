db_username: ${DB_USER:postgres}
db_password: ${DB_PASSWORD:EDGG3gD-2b*f5D434G4fCbDDG-23fB*2}
db_uriName: ${DB_NAME:railway}
db_host: ${DB_HOST:monorail.proxy.rlwy.net:10806}
server:
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: G26Dorixona
    keyStoreType: PKCS12
    keyAlias: myapp
  port: 9090
spring:
  redis:
    url: jdbc:redis://monorail.proxy.rlwy.net:16690
  mail:
    host: smtp.gmail.com
    port: 587
    username: yvycvydfgg@gmail.com
    password: jvrlucfxwbeevdnp
    properties:
      mail.smtp.starttls.enable: true
      mjaail.smtp.auth: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${db_username}
    password: ${db_password}
    url: jdbc:postgresql://${db_host}/${db_uriName}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
jwt:
  expiry: 30000000
  secret: asdfghjkl;'';lkjhgfdsaqwertyuiop[[poiuytrewqzxcvbnm,.,mnbvcxz



logging:
  level:
    org:
      hibernate:
        SQL=DEBUG:
